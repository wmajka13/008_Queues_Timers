# Pliki kompilacji i pliki binarne (wyniki z CMake/Ninja/Make)
build/
Debug/
Release/
*.o
*.elf
*.bin
*.hex
*.map
*.su
*.lst

# Pliki tymczasowe CMake i narzędzi
.cache/
compile_commands.json
CMakeCache.txt
CMakeFiles/
cmake_install.cmake

# Ustawienia środowiska graficznego (opcjonalne)
# Jeśli chcesz, aby współpracownicy mieli te same ustawienia VS Code, 
# usuń .vscode/ z ignorowanych.
.vscode/
.settings/
.project
.cproject
